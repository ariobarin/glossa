# Glossa review workspace

This disposable workspace contains public, deterministic files for plugin review. It contains no credentials, private source code, or network-dependent tests.

The sample module exports `add` and `multiply`. Run its checks with `npm test`.
